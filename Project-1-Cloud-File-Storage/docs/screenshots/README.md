# Screenshots

This folder is a placeholder. After you deploy the application (locally via
Docker Compose or to AWS), capture the following screenshots and drop them
here as `.png` files for the internship submission:

1. `01-login.png` — the Google Sign-In screen.
2. `02-dashboard-empty.png` — the dashboard immediately after first login (empty state).
3. `03-upload-progress.png` — a file mid-upload with the progress bar visible.
4. `04-file-grid.png` — the dashboard with several files and at least one folder.
5. `05-version-history.png` — the version history modal for a file that has been re-uploaded at least twice.
6. `06-share-dialog.png` — the share dialog showing an active share link.
7. `07-shared-link-view.png` — the public `/shared/:token` page opened in an incognito window.
8. `08-s3-console-versioning.png` — the AWS S3 console showing "Versioning: Enabled" on the bucket.
9. `08-rds-console.png` (Project 6 reuse if applicable).

No screenshots are included in this repository — they must be captured
against a real running deployment, not fabricated.
